{% extends 'layout.twig' %}

{% set site_title = post.title %}

{% block page %}
  
  {% include 'components/banner.twig' with {
    type: 'shop',
    title: site_title,
    image: post.get_field('background_image').url
  } %}

{% endblock %}
